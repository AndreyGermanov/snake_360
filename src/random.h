#pragma once 
using namespace std;

/// Возвращает произвольное число из указанного диапазона
int rand(int min, int max);

/// Возвращает произвольную координату (x,y) в указанных пределах
pair<int,int> getRandPos(int maxX, int maxY);
